<Window x:Class="TimmyTools.WpfUi.Views.BreakTimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TimmyTools.WpfUi.ViewModels"
        Title="Break Timer"
        Width="600" Height="460"
        MinWidth="400" MinHeight="310"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:BreakTimerViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="TimerButtonBase" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#555555"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#666666"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PresetButtonStyle" TargetType="Button" BasedOn="{StaticResource TimerButtonBase}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>

        <!-- Small icon button for top-right toolbar -->
        <Style x:Key="ToolbarIconButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Background" Value="#333333"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <Setter Property="Background" Value="#2E7D32"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <DockPanel LastChildFill="True">

        <!-- Top Bar: Class Title + Toolbar Icons (Pause/Reset/Settings) -->
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Class Title (centered in middle column) -->
            <TextBlock Grid.Column="1"
                       Text="{Binding ClassTitle}"
                       FontSize="48" FontFamily="Segoe UI Light"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       Margin="0,4,0,0"/>

            <!-- Toolbar Icons: upper-right -->
            <StackPanel Grid.Column="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="0,6,8,0">
                <!-- Pause (visible when running) -->
                <Button Content="&#xE769;"
                        Style="{StaticResource ToolbarIconButtonStyle}"
                        Command="{Binding PauseCommand}"
                        ToolTip="Pause"
                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"/>
                <!-- Resume (visible when paused) -->
                <Button Content="&#xE768;"
                        Style="{StaticResource ToolbarIconButtonStyle}"
                        Command="{Binding ResumeCommand}"
                        ToolTip="Resume"
                        Visibility="{Binding IsPaused, Converter={StaticResource BoolToVis}}"/>
                <!-- Reset (visible when active) -->
                <Button Content="&#xE72C;"
                        Style="{StaticResource ToolbarIconButtonStyle}"
                        Command="{Binding ResetCommand}"
                        ToolTip="Reset"
                        Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"/>
                <!-- Settings (always visible) -->
                <Button x:Name="SettingsButton"
                        Content="&#xE713;"
                        Style="{StaticResource ToolbarIconButtonStyle}"
                        Click="SettingsButton_Click"
                        ToolTip="Quick edit Class and Next Up"/>
                <Popup x:Name="QuickEditPopup"
                       PlacementTarget="{Binding ElementName=SettingsButton}"
                       Placement="Bottom"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       HorizontalOffset="-540"
                       VerticalOffset="4"
                       Opened="QuickEditPopup_Opened"
                       Closed="QuickEditPopup_Closed">
                    <Border Background="#2A2A2A"
                            BorderBrush="#555555"
                            BorderThickness="1"
                            CornerRadius="4"
                            MinWidth="560"
                            Padding="14">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="Class"
                                       Foreground="#CCCCCC"
                                       VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     x:Name="ClassTitleTextBox"
                                     Background="#1E1E1E"
                                     Foreground="White"
                                     BorderBrush="#555555"
                                     CaretBrush="White"
                                     Padding="6,4"
                                     FontSize="14"
                                     Text="{Binding BreakTimerSettings.ClassTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     KeyDown="QuickEditTextBox_KeyDown"/>
                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="Segment"
                                       Foreground="#CCCCCC"
                                       VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1"
                                     Background="#1E1E1E"
                                     Foreground="White"
                                     BorderBrush="#555555"
                                     CaretBrush="White"
                                     Padding="6,4"
                                     FontSize="14"
                                     Text="{Binding Segment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     KeyDown="QuickEditTextBox_KeyDown"/>
                            <TextBlock Grid.Row="4" Grid.Column="0"
                                       Text="Topic"
                                       Foreground="#CCCCCC"
                                       VerticalAlignment="Center"/>
                            <TextBox Grid.Row="4" Grid.Column="1"
                                     Background="#1E1E1E"
                                     Foreground="White"
                                     BorderBrush="#555555"
                                     CaretBrush="White"
                                     Padding="6,4"
                                     FontSize="14"
                                     Text="{Binding BreakTimerSettings.NextUp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     KeyDown="QuickEditTextBox_KeyDown"/>
                        </Grid>
                    </Border>
                </Popup>
            </StackPanel>
        </Grid>

        <!-- Next Up footer: composed in the ViewModel as "Next Up | Segment X | Topic" -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center"
                    Margin="0,0,0,20"
                    Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding NextUpDisplay}"
                       FontSize="56" FontFamily="Segoe UI Light"
                       Foreground="#CCCCCC"/>
        </StackPanel>

    <Viewbox Stretch="Uniform" Margin="20">
        <Grid Width="560" Height="320">

            <!-- Idle State: Preset Selection -->
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="{Binding IsIdle, Converter={StaticResource BoolToVis}}">

                <!-- Preset Buttons -->
                <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Button Content="5 min" Style="{StaticResource PresetButtonStyle}"
                            Command="{Binding StartPresetCommand}"
                            CommandParameter="5"/>
                    <Button Content="10 min" Style="{StaticResource PresetButtonStyle}"
                            Command="{Binding StartPresetCommand}"
                            CommandParameter="10"/>
                    <Button Content="30 min" Style="{StaticResource PresetButtonStyle}"
                            Command="{Binding StartPresetCommand}"
                            CommandParameter="30"/>
                    <Button Content="45 min" Style="{StaticResource PresetButtonStyle}"
                            Command="{Binding StartPresetCommand}"
                            CommandParameter="45"/>
                </WrapPanel>

                <!-- Custom Input -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Custom:"
                               FontSize="18" Foreground="#AAAAAA"
                               VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Text="{Binding CustomMinutes, UpdateSourceTrigger=PropertyChanged}"
                             Width="60" FontSize="18"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center"
                             Padding="4"/>
                    <TextBlock Text="min"
                               FontSize="18" Foreground="#AAAAAA"
                               VerticalAlignment="Center" Margin="10,0,10,0"/>
                    <Button Content="Start" Style="{StaticResource PresetButtonStyle}"
                            Command="{Binding StartCustomCommand}"/>
                </StackPanel>
            </StackPanel>

            <!-- Active State: Running / Paused / Completed -->
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}">

                <!-- Countdown / Break Over Text -->
                <Grid HorizontalAlignment="Center" Margin="0,0,0,8">
                    <!-- Countdown (visible when not completed) -->
                    <TextBlock Text="{Binding TimeRemainingText}"
                               FontSize="84"
                               FontFamily="Segoe UI Light"
                               Foreground="White"
                               HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Break Over (visible when completed) -->
                    <TextBlock Text="Break Over"
                               FontSize="84"
                               FontFamily="Segoe UI Light"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVis}}"/>
                </Grid>

                <!-- Verbose Time Text -->
                <TextBlock Text="{Binding VerboseTimeText}"
                           FontSize="24"
                           Foreground="#AAAAAA"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Progress Bar -->
                <Grid Height="30" Margin="0,0,0,20">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- Background -->
                    <Border Background="#555555" CornerRadius="4"/>
                    <!-- Elapsed fill -->
                    <Border Background="#4CAF50" CornerRadius="4"
                            HorizontalAlignment="Left">
                        <Border.Width>
                            <MultiBinding>
                                <MultiBinding.Converter>
                                    <conv:FractionToWidthConverter xmlns:conv="clr-namespace:TimmyTools.WpfUi.Converters"/>
                                </MultiBinding.Converter>
                                <Binding Path="ProgressFraction"/>
                                <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}"/>
                            </MultiBinding>
                        </Border.Width>
                    </Border>
                </Grid>
            </StackPanel>

        </Grid>
    </Viewbox>

    </DockPanel>
</Window>
